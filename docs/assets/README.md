# Portrait asset

Save a headshot here as `me.jpg` (or me.png and update PORTRAIT_SRC in ../index.html).
The interactive terminal converts it to braille dot-art on load and via the `me` command.
Square-ish, well-lit, high-contrast photos convert best.
